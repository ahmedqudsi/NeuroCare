// This file is neutralized to prevent conflicts with the new [locale] routing structure.
import type { ReactNode } from 'react';

export default function AppLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
